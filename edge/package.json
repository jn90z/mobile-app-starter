{
  "name": "mobile-app-starter-edge",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:local": "wrangler d1 migrations apply app-db --local",
    "db:remote": "wrangler d1 migrations apply app-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.2",
    "wrangler": "^4.0.0"
  }
}
