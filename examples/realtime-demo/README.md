# Realtime Demo

Reference flow for two authenticated Android clients exchanging live events.

The demo is intentionally transport-focused: authenticate, connect, send an event,
receive the event on another client, disconnect, reconnect, and resynchronize.
Use this to validate the platform realtime contract before adding product UI.
